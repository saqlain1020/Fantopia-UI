import React from 'react'
import { Route, Switch } from 'react-router'

const Routes = () => {
    return (
        <Switch>
            <Route path="/" exact/>    
        </Switch>
    )
}

export default Routes
